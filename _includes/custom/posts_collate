<ul>
  {% for post in posts_collate  %}
    <li><span>{{ page.date | date_to_string }}</span> &raquo; <a href="{{ BASE_PATH }}{{ post.url }}">{{ post.title }}</a></li>
  {% endfor %}
</ul>

{% assign posts_collate = nil %}